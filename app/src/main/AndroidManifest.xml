<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.INTERNET"/>


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.DailyFlowAI"
        tools:targetApi="31">
        <activity
            android:name=".ui.DashboardActivity.DashboardActivity"
            android:exported="false" />
        <activity
            android:name=".ui.signupActivity.SignUpActivity"
            android:exported="false" />
        <activity
            android:name=".ui.loginAcivity.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".ui.onboardActivity.OnboardActivity"
            android:exported="false" />
        <activity
            android:name=".ui.luancherActivity.LuancherActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.mainActivity.MainActivity"
            android:exported="false" />
    </application>

</manifest>